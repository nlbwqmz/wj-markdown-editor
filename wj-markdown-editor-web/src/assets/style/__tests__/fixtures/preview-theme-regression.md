# 预览主题回归样本

这是一个覆盖常见渲染元素的段落，包含 **粗体**、*斜体* 与 [链接](https://example.com)。

> 这是一段引用内容，用于检查引用块样式。

---

1. 第一项
2. 第二项
3. 第三项

- 无序项 A
- 无序项 B
- 无序项 C

- [x] 已完成任务
- [ ] 未完成任务

| 列一 | 列二 | 列三 |
| --- | --- | --- |
| 表头 | 内容 | 样式 |
| 对齐 | 检查 | 正常 |

![示例图片](./assets/example.png)

!audio(./assets/example.mp3)

!video(./assets/example.mp4)

这里有一段行内代码 `const theme = 'github'` 用于检查代码样式。

```js
function previewThemeSample() {
  return 'github'
}
```

这是一个脚注引用。[^preview-theme]

> [!TIP]
> 这是一个 GitHub Alert，用于检查提示框样式。

::: details 点击展开详情
这里是 details 容器的内容。
:::

[^preview-theme]: 这是脚注内容。
